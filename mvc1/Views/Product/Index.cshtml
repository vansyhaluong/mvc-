@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model List<mvc1.Models.Product>
<h2>Danh sách sản phẩm</h2>
<a class="btn btn-primary mb-3" asp-controller="Product" asp-action="Create">Thêm mới</a>
<table cellpadding="10"
       class="w-50"
       style="
            border-radius:10px;
            overflow:hidden;
            border-collapse:separate;
            border-spacing:0;
            border:1px solid #ccc;">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Price</th>
        <th>Action</th>

    </tr>
    @foreach(var product in Model)
    {
        <tr>
            <td>@product.Id</td>
            <td>@product.Name</td>
            <td>@product.Price</td>
            <td >
                <a class="btn btn-warning" asp-action="Edit"  asp-route-id="@product.Id">Sửa</a>
                <a class="btn btn-danger" asp-action="Delete" asp-route-id="@product.Id">Xóa</a>
            </td>
        </tr>
    }

</table>